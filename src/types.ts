export type Credentials = {
  email: string;
  password: string;
};

export type SelfT = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  role: string;
  updatedAt: Date;
  createdAt: Date;
};
